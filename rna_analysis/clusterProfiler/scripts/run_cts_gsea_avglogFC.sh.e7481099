Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  9.29% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning message:
In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (22.08% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  9.29% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (18.35% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  11.22% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (39.76% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  11.22% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (35.2% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  10.73% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (55.32% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 7 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  10.73% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (51.65% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  7.74% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning message:
In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (74.91% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  7.74% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (73.35% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  11.41% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (14.57% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 16 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  11.41% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (10.81% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  10.84% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (8.6% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 4 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  10.84% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (6.2% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  There were 7 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
5: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  8.02% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (34.38% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 12 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  8.02% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (31.07% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  11.05% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (39.51% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 7 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  11.05% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (35.04% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  8.34% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (20.72% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 1 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  8.34% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (17.72% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  There were 2 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  9.67% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (33.19% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 1 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In selectChildren(jobs, timeout) : error 'No child processes' in select
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  9.67% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (29.09% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some of the pathways the P-values were likely overestimated. For such pathways log2err is set to NA.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:1 mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  8.89% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning message:
In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (80.98% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
'select()' returned 1:1 mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  8.89% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (79.6% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  9.45% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning message:
In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (68.92% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  9.45% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (66.54% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
Warning message:
package ‘argparse’ was built under R version 4.0.5 
'select()' returned 1:many mapping between keys and columns
Warning message:
In bitr(markers$gene, fromType = "SYMBOL", toType = "ENTREZID",  :
  10.79% of input gene IDs are fail to map...
preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (30.15% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
2: In fgseaMultilevel(...) :
  There were 8 pathways for which P-values were not calculated properly due to unbalanced (positive and negative) gene-level statistic values. For such pathways pval, padj, NES, log2err are set to NA. You can try to increase the value of the argument nPermSimple (for example set it nPermSimple = 10000)
3: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
'select()' returned 1:many mapping between keys and columns
Reading KEGG annotation online:

Reading KEGG annotation online:

Reading KEGG annotation online:

preparing geneSet collections...
GSEA analysis...
leading edge analysis...
done...
Warning messages:
1: In bitr(names(gene.list), fromType = "SYMBOL", toType = "ENTREZID",  :
  10.79% of input gene IDs are fail to map...
2: In kegg.ranks == gene.list :
  longer object length is not a multiple of shorter object length
3: In preparePathwaysAndStats(pathways, stats, minSize, maxSize, gseaParam,  :
  There are ties in the preranked stats (25.79% of the list).
The order of those tied genes will be arbitrary, which may produce unexpected results.
4: In fgseaMultilevel(...) :
  For some pathways, in reality P-values are less than 1e-10. You can set the `eps` argument to zero for better estimation.
